{
  "name": "agent-skills-repository",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Project-local Agent Skills distributed through npx skills",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "validate": "node scripts/validate-skills.mjs",
    "test": "node tests/setup-heroui-pro.test.mjs && node tests/light-ocr-text-extraction.test.mjs",
    "check": "npm run validate && npm test"
  }
}
